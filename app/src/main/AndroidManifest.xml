<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.com.zhihetech.online">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!--环信权限配置-->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!--<uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION" />-->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:name=".core.view.ZhiheApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.widget.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.widget.MainActivity" />
        <activity
            android:name=".ui.widget.DailyNewActivity"
            android:label="@string/daily_new"></activity>
        <activity
            android:name=".ui.widget.MerchantHomeActivity"
            android:label="@string/merchant_home"></activity>
        <activity
            android:name=".ui.widget.GoodsInfoActivity"
            android:label="@string/goods_detail"></activity>
        <activity
            android:name=".ui.widget.CategoryActivity"
            android:label="@string/category"></activity>
        <activity android:name=".ui.widget.CategoryDetailActivity" />
        <activity android:name=".ui.widget.SearchActivity" />
        <activity
            android:name=".ui.widget.ReceiptAddressActivity"
            android:label="@string/receipt_address" />
        <activity
            android:name=".ui.widget.AddOrEditReceiptActivity"
            android:label="@string/add_receipt_address" />
        <activity
            android:name=".ui.widget.OrderConfirmActivity"
            android:label="@string/order_confirm" />
        <activity
            android:name=".ui.widget.OrderActivity"
            android:label="@string/my_order" />
        <activity
            android:name=".ui.widget.OrderDetailActivity"
            android:label="@string/order_detail" />

        <!--Ping++支付配置开始-->
        <!-- Ping++ SDK -->
        <activity
            android:name="com.pingplusplus.android.PaymentActivity"
            android:configChanges="orientation|screenSize"
            android:launchMode="singleTop"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- 微信支付 -->
        <activity-alias
            android:name=".wxapi.WXPayEntryActivity"
            android:exported="true"
            android:targetActivity="com.pingplusplus.android.PaymentActivity" />

        <!-- 支付宝 -->
        <activity
            android:name="com.alipay.sdk.app.H5PayActivity"
            android:configChanges="orientation|keyboardHidden|navigation"
            android:exported="false"
            android:screenOrientation="behind"></activity>
        <activity
            android:name="com.alipay.sdk.auth.AuthActivity"
            android:configChanges="orientation|keyboardHidden|navigation"
            android:exported="false"
            android:screenOrientation="behind"></activity>

        <!-- 银联支付 -->
        <activity android:name="com.unionpay.uppay.PayActivity" />
        <!--Ping++支付配置结束-->

        <!-- 设置环信应用的appkey -->
        <meta-data
            android:name="EASEMOB_APPKEY"
            android:value="0871zhkj#zhihedianshang" />
        <!-- 声明环信sdk所需的service SDK核心功能-->
        <service android:name="com.easemob.chat.EMChatService" />
    </application>

</manifest>
